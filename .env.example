## Swift Auth package environment configuration
## Keep only package-scoped variables here. Host application env (DB, MAIL,
## APP_*, etc.) should live in the host project's `.env` file, not in this
## package example.

## Frontend stack: one of "typescript", "javascript", "blade". Default: typescript
SWIFT_AUTH_FRONTEND=typescript

## Redirect after successful authentication (relative path)
SWIFT_AUTH_SUCCESS_URL=/

## Password reset token TTL, in seconds (default 900 = 15 minutes)
SWIFT_AUTH_PASSWORD_RESET_TTL=900

## Optional: prefix applied to package database table names. Leave empty for
## no prefix. Example: `swift_auth_` will create tables named
## `swift_auth_users`, `swift_auth_roles`, etc.
SWIFT_AUTH_TABLE_PREFIX=swift-auth_

## Route URL and name prefix for package routes. Default: `swift-auth`
SWIFT_AUTH_ROUTE_PREFIX=swift-auth
